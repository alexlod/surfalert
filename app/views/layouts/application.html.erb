<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Surfalert</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Get a phone call when the surf is good.">
  
  <meta property="og:title" content="Surfalert" />
  <meta property="og:site_name" content="Surfalert" />
  <meta property="og:description" content="et a phone call when the surf is good." />
  
  <%= stylesheet_link_tag "application" %>
  <%= csrf_meta_tags %>
  
  <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!--[if lt IE 9]>
    <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
</head>
<body>

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="brand" href="/">Surfalert</a>
        <ul class="nav pull-right">
          <% if user_logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to "/logout" do %>
                    <i class="icon-off"></i> Sign Out
                  <% end %>
                </li>
              </ul>
            </li>
          <% else %>
            <li>
              <%= link_to "Signup", new_user_path %>
            </li>
            <li>
              <%= link_to "Login", "/login" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

  <div class="container">
    
    <%= notice_msg(flash[:notice]) if flash[:notice] %>
    <%= alert_msg(flash[:alert]) if flash[:alert] %>
  
    <%= yield %>

    <hr>
    <footer>
      <p>
        &copy; 2012 &middot; Surfalert
      </p>
    </footer>

  </div>

<%= yield :javascript %>
<%= javascript_include_tag "application" %>

</body>
</html>
